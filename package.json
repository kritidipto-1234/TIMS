{
  "name": "TIMS",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "parcel index.html --out-dir docs --public-url ''",
    "build": "parcel build index.html --out-dir docs --public-url ./",
    "deploy": "git add . && git commit -a -m 'some changes' && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  }
}
